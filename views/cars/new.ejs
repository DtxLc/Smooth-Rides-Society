<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Smooth Rides Society</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <%- include('../partials/header') %>
    
    <% if (locals.error) { %>
      <div class="alert alert-error">
        <%= error %>
      </div>
    <% } %>
    
    <% if (locals.success) { %>
      <div class="alert alert-success">
        <%= success %>
      </div>
    <% } %>
    
    <main>
      <section class="section">
        <div class="section-container">
          <div class="auth-header">
            <a href="/cars" class="back-to-home">
              <i class="fas fa-arrow-left"></i>
              <span>Back to Cars</span>
            </a>
            <h1>Add New Car</h1>
          </div>
          
          <div class="auth-container">
            <form action="/cars" method="POST" class="auth-form">
              <div class="form-group">
                <label for="make">Make</label>
                <input 
                  type="text" 
                  id="make" 
                  name="make" 
                  required 
                  value="<%= locals.car ? car.make : '' %>"
                  placeholder="e.g., Ferrari, Lamborghini, Porsche"
                  autofocus
                >
              </div>
              
              <div class="form-group">
                <label for="model">Model</label>
                <input 
                  type="text" 
                  id="model" 
                  name="model" 
                  required
                  value="<%= locals.car ? car.model : '' %>"
                  placeholder="e.g., 488 GTB, Huracán, 911"
                >
              </div>
              
              <div class="form-row">
                <div class="form-group">
                  <label for="year">Year</label>
                  <input 
                    type="number" 
                    id="year" 
                    name="year" 
                    required
                    min="1885"
                    max="<%= new Date().getFullYear() + 1 %>"
                    value="<%= locals.car ? car.year : new Date().getFullYear() %>"
                  >
                </div>
                
                <div class="form-group">
                  <label for="color">Color</label>
                  <input 
                    type="text" 
                    id="color" 
                    name="color" 
                    required
                    value="<%= locals.car ? car.color : '' %>"
                    placeholder="e.g., Rosso Corsa, Yellow, Black"
                  >
                </div>
              </div>
              
              <div class="form-row">
                <div class="form-group">
                  <label for="price">Price ($)</label>
                  <input 
                    type="number" 
                    id="price" 
                    name="price" 
                    required
                    min="0"
                    step="0.01"
                    value="<%= locals.car ? car.price : '' %>"
                    placeholder="299999"
                  >
                </div>
                
                <div class="form-group">
                  <label for="horsepower">Horsepower</label>
                  <input 
                    type="number" 
                    id="horsepower" 
                    name="horsepower" 
                    required
                    min="0"
                    value="<%= locals.car ? car.horsepower : '' %>"
                    placeholder="650"
                  >
                </div>
              </div>
              
              <div class="form-row">
                <div class="form-group">
                  <label for="topSpeed">Top Speed (mph)</label>
                  <input 
                    type="number" 
                    id="topSpeed" 
                    name="topSpeed"
                    min="0"
                    step="0.1"
                    value="<%= locals.car ? car.topSpeed : '' %>"
                    placeholder="205"
                  >
                </div>
                
                <div class="form-group">
                  <label for="zeroToSixty">0-60 mph (seconds)</label>
                  <input 
                    type="number" 
                    id="zeroToSixty" 
                    name="zeroToSixty"
                    min="0"
                    step="0.1"
                    value="<%= locals.car ? car.zeroToSixty : '' %>"
                    placeholder="3.0"
                  >
                </div>
              </div>
              
              <div class="form-group">
                <label for="imageUrl">Image URL</label>
                <input 
                  type="url" 
                  id="imageUrl" 
                  name="imageUrl"
                  placeholder="https://example.com/image.jpg"
                  value="<%= locals.car ? car.imageUrl : '' %>"
                >
              </div>
              
              <div class="form-group">
                <label for="description">Description</label>
                <textarea 
                  id="description" 
                  name="description" 
                  rows="4"
                  placeholder="Tell us about this amazing car..."
                ><%= locals.car ? car.description : '' %></textarea>
              </div>
              
              <div class="form-actions" style="display: flex; gap: 1rem; justify-content: space-between; margin-top: 2rem;">
                <a href="/cars" class="btn-secondary">
                  <i class="fas fa-times"></i>
                  Cancel
                </a>
                <button type="submit" class="btn-primary">
                  <i class="fas fa-plus"></i>
                  Add Car
                </button>
              </div>
            </form>
          </div>
        </div>
      </section>
    </main>
    
    <%- include('../partials/footer') %>
  </div>
  
  <script src="/main.js"></script>
</body>
</html>
